const UserId = 1234 // remain constant in whole program
let userName = "Player1"
var userAddress = "asdadad" // try not to use it as it dont now the scope of extection like only in this {} it should execute
userCountry = "India" // we can also declare like this but try to avoid it 

console.log("UserId");
console.table([UserId,userName,userAddress,userCountry])
